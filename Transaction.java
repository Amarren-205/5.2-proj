/**
 * Transactions interface defines deposit and withdraw methods
 */
public interface Transaction {
    void deposit(double amount);
    void withdrawl(double amount);  // intentional spelling to match original code
}
